polling strategy

reset fifos extern
reset fifo in ezusb

set DACs
start gpif, start usb

start sampling w Timed_Inhibit

inner loop: 
      check if timer is still running (with TimeToGo !=0)
      check if bytes have arrived (with get_Transferredbytes)

do a manual flush
      pick up data and process

shut down usb
shut down gpif

close device
